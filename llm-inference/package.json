{
  "name": "llm-runtime",
  "version": "1.0.3",
  "type": "module",
  "license": "Apache-2.0",
  "description": "Local LLM inference + autonomous agent runtime — GPU accelerated",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "tauri": "tauri",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "test": "node src/artifact.test.js && node src/contract.test.js",
    "lint": "svelte-check --tsconfig ./tsconfig.json --fail-on-warnings",
    "ci": "npm run check && npm run test && npm run build"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.4",
    "@tauri-apps/cli": "^2",
    "svelte": "^5.55.9",
    "svelte-check": "^4.4.8",
    "typescript": "^6.0.3",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2",
    "@tauri-apps/plugin-fs": "^2",
    "@tauri-apps/plugin-shell": "^2",
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "highlight.js": "^11.11.1",
    "marked": "^18.0.4",
    "xterm-addon-fit": "^0.8.0"
  }
}
